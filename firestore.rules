rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Permite lectura/escritura pública (sin inicio de sesión para el TFM)
      allow read, write: if true;
    }
  }
}
