rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Permite lectura/escritura solo a usuarios autenticados (anónimos o normales)
      allow read, write: if request.auth != null;
    }
  }
}
